
vagrant list-commands -> liste des commdes disponibles 
vagrant list-commands -h 

vagrant global-status -> voir le statut des ressources déployées

Lab-1
-----
vagrant init ubuntu/trusty64 -> initialisation avec iso ubuntu/trusty64
vagrant init ubuntu/trusty64 --box-version=20190425.0.0


vagrant up -> provisionner la machine

par défauutisateur vagrant sera crée
connxion ssh (pair de clés shh crées)

vagrant status -> voir le statut des ressources par rapport au reppertoire du vagrantfile

vagrant ssh -> connexion ssh

vagrant halt -> arrêt de la vm

vagrant destroy >suppression de la vm


Vagrant Boxes
--------------

Contient: Box file(iso à utiliser), Box catalog metadata, Box information(auteur, entreprise,..)

vagrant box add ubuntu/trusty64
vagrant box add ubuntu/trusty64 --box-version=20190419.0.1

Vagrant cloud : herberger des boxes


Lab-2 - Vagrant Box
-------------------

vagrant init ubuntu/trusty64

vagrant up

vagrant ssh 

sudo apt-get update
sudo apt-get install nginx -y
sudo service nginx start
sudo service nginx status
curl 127.0.0.1

publication de vbox:

vagrant package --output nginx.box

upload de la vbox et réalisez la release

utilisation de la vbox:
vagrant init orpheadya/nginx --box-version 1.0
vagrant up


Lab3 - Vagrantfile
-------------------

vagrant init geerlingguy/centos7 -m


vagrant validate
vagrant up
vagrant ssh 
sudo yum install nginx -y
sudo systemctl start nginx 
sudo systemctl statusnginx 
curl 127.0.0.1
vagrant destroy -f



Lab4 - Networking @ ip prvivée 10.0.0.10
-------------------------------------

vagrant init geerlingguy/centos7 -m


vagrant validate
vagrant up
ssh vagrant@10.0.0.10
[vagrant@localhost ~]$

sudo yum install nginx -y
sudo systemctl start nginx 
sudo systemctl status nginx 
curl 10.0.0.10
vagrant destroy -f


Lab-5 Multi Machines
--------------------

Infra avec plusieurs serveurs
Architectures Distribuées
Tester un desaster recovery



Lab-6 Plugins
-------------

vagrant plugin -h

vagrant plugin install vagrant-hostsupdater

vagrant ssh lb




Lab-7 Files
-----

Partage des fichiers entre hote et VM par montage ou copie

config.vm.synced_folder "website/", "/usr/share/nginx/html/"




Provisionning
-------------

Exécution des scripts post creation des VM

Différant cas de demande de provisionning:
vagrant up
vagrant up --provision
vagrant provision
vagrant reload --provision



























































































